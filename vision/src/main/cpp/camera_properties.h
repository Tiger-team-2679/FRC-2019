#pragma once
#include "util.h"

constexpr int WIDTH = 640;
constexpr int HEIGHT = 480;
constexpr int FPS = 30;
constexpr float HORIZONTAL_FOV = rad(50);
constexpr float VERTICAL_FOV = rad(40);
constexpr float CARGO_SIZE;
constexpr float CAMERA_PHISICAL_HEIGHT;
constexpr float STRIPS_PHISICAL_HEIGHT;